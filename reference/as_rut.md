# Formatear una cadena de texto o numero en formato RUT

Formatear una cadena de texto o numero en formato RUT

## Usage

``` r
as_rut(rut)
```

## Arguments

- rut:

  RUT con o sin digito verificador, con o sin guion y/o puntos.

  ruttexto_rutnum("16.019.432")
